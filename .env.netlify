# Netlify Environment Variables Template
# Copy these to your Netlify Dashboard → Site Settings → Environment Variables

# Application Environment
NODE_ENV=production

# Security
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# Database (Netlify Functions will use temporary storage)
DATABASE_PATH=/.netlify/functions/data/bd-ticketpro.db

# Optional: Custom configuration
APP_NAME=BD TicketPro
APP_VERSION=1.0.0

# IMPORTANT NOTES:
# 1. Never commit actual secret values to git
# 2. Generate a strong JWT_SECRET (minimum 32 characters)
# 3. Set these variables in Netlify Dashboard, not in this file
# 4. This file is just a template for reference
